## Resonators

* CV2 and Knob 2 should control feedback, with 0 at midpoint, positive increasing, inverted decreasing 
* wavefolding should remain on the menu with default 0%
* The Wiring menu can be removed
* the Delay algorithm has much lower output than the other models
* there always appears to be Y-X feedback in DSP, that should default to zero, as should X-Y
* there should be an Offset menu item on the Reonators menu that adjusts the ratio of the pitch of the second delay to the first, default 1:1
* MIDI needs fixing. Review dirac, that worked